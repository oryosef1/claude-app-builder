import { describe, it, expect } from 'vitest';
// Import types to ensure they're included in coverage
import type * as Types from '../../src/types/index.js';

describe('Type Definitions', () => {
  it('should have type definitions', () => {
    // This test just ensures types are imported and processed
    expect(true).toBe(true);
  });
});